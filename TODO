* at tactics for refinement
* tactics for substituting for fields in refinements
* finalizeRecordType
* finalizeInductiveType
* improve parsing for Spec Variable foo : T  so we don't need parens around T
* Add form: Spec Interpretation foo : spec1 -> spec2 := bar, to allow
  interpretations to be defined from instances (and not just vice-versa)
* Figure out how to keep the op names in prove_simple_interp
